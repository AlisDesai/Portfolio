# Public site URL, used for metadata, sitemap, and canonical links.
NEXT_PUBLIC_SITE_URL=http://localhost:3008

# Base URL for the API layer (lib/api). Leave empty if not used yet.
NEXT_PUBLIC_API_URL=
